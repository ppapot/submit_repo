#this makefile insure that after clone of the build directory the following action are done :
#   - make logread executable
#   - make logappend executable
#   - create the logapplication folder in order to store the log of the apps

default :
	chmod +x logappend logread

clean : 
	rm -f *.pyc
